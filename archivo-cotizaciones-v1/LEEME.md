# Cotizaciones (versión anterior del laboratorio) — archivo de referencia

Esta carpeta guarda la versión anterior del laboratorio (app de "Gestión de
Cotizaciones": API + web + capas + SQLite, con checkpoints `cp-00`…`cp-07`).
Se conserva como **referencia**; ya no es el laboratorio activo del taller.

El laboratorio vigente vive en la **raíz** del repo: una landing con formulario
de contacto y un panel con una falla de control de acceso plantada. Ver el
`README.md` de la raíz.
